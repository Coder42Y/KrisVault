# {{ repo_name }} 部署指南

> 仓库地址：{{ repo_url }}
> 分支：{{ branch }}
> 调研时间：{{ generated_at }}

---

## 部署方式

{{ deployment.description }}

## 环境变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
{% for env in deployment.env_vars %}
| `{{ env.name }}` | {{ env.description }} | {{ env.default or "-" }} |
{% endfor %}

## Docker 部署

```bash
{{ deployment.docker_command or "# 未找到 Docker 部署方式" }}
```

## 配置文件示例

```yaml
{{ deployment.config_example or "# 未找到配置示例" }}
```

## 总结

{{ summary }}
